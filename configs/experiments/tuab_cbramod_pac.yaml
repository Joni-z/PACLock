name: tuab-cbramod_pac
group: D
dataset: tuab
data_root: /work1/chenyuyou/yifanwang/Zhizhe/processed_pac/tuab
model: cbramod
loader: null
pretrained: true
optimizer: adamw
lr: 0.0001
weight_decay: 0.05
epochs: 50
batch_size: 64
multi_lr: true
scheduler: cosine
grad_clip: 1.0
patience: 0
num_workers: 16
device: cuda
eval_every_steps: 0
model_kwargs:
  dropout: 0.1
num_classes: 2
sample_rate: 200
loss: bce_with_logits
seed: 0
_pac_note: 'PAC-methodology protocol: 0.5 Hz high-pass, no notch. Identical to the
  frozen config in every other respect.'
select_metric: auroc
