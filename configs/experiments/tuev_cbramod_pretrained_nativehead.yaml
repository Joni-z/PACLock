name: tuev-cbramod_pretrained_nativehead
group: B
dataset: tuev
data_root: $PACLOCK_PROC/processed/tuev
model: cbramod
loader: null
pretrained: true
optimizer: adamw
lr: 0.0001
weight_decay: 0.05
epochs: 50
batch_size: 64
multi_lr: true
scheduler: cosine
grad_clip: 1.0
patience: 0
num_workers: 16
device: cuda
eval_every_steps: 0
model_kwargs:
  dropout: 0.1
  classifier_hidden_dim: 1000
num_classes: 6
sample_rate: 200
loss: cross_entropy
label_smoothing: 0.1
seed: 0
screening_note: Prepared only. Correct upstream TUEV all_patch_reps hidden width; keep historical results separate. No training admitted by this configuration.
