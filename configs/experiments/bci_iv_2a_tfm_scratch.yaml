name: bci_iv_2a-tfm_scratch
group: C
dataset: bci_iv_2a
data_root: $PACLOCK_PROC/processed/bci_iv_2a
model: tfm
loader: tfm
pretrained: false
tfm_setting: multiple
optimizer: adamw
lr: 0.001
weight_decay: 1.0e-05
epochs: 50
batch_size: 16
warmup_epochs: 5
label_smoothing: 0.1
scheduler: cosine
grad_clip: null
patience: 10
num_workers: 16
device: cuda
eval_every_steps: 0
num_classes: 4
loss: cross_entropy
seed: 0
