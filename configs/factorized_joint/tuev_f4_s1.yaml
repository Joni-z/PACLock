# Prepared only; no training admission. Fixed 128/32/32 coupling/band/broadband coordinates.
batch_size: 32
data_root: $PACLOCK_PROC/processed/tuev
dataset: tuev
device: cuda
epochs: 20
eval_every_steps: 0
grad_clip: 1.0
group: factorized_joint_confirm
label_smoothing: 0.1
loss: cross_entropy
lr: 0.0001
max_hours: 5
model: paclock
model_kwargs:
  arch: triaxial
  augmentations: []
  band_pe: index
  content_scale: 1.0
  content_source: band_broadband
  coupling_dim: 128
  d_model: 192
  depth: 6
  dropout: 0.2
  freq_mixer: attention
  interaction_mode: rotation
  kernel_size: 201
  n_bands: 8
  n_heads: 4
  pac_patch_len: 50
  pac_token_mode: measured
  patch_len: 50
  spatial_pe: xyz
  tokenizer_mode: factorized
name: tuev-factorized_f4_joint_confirm
num_classes: 6
num_workers: 8
optimizer: adamw
patience: 20
sample_rate: 200
scheduler: cosine
seed: 1
weight_decay: 1.0e-05
screening_note: Prepared conditionally for paired seed-1 confirmation on an existing
  mi2104x allocation. Admit only after the seed-0 TUEV pair completes and is reviewed.
  Compare within runtime cohort; do not silently pool with mi2101x seed 0.
