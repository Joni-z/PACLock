# CF1 on the RELEASED CBraMod checkpoint (2026-09-09): pretrained weights + 8 extra rows (add_cpl).
# Recipe copied from the corpus's cbramod_pretrained row. Single seed.
name: tuar-cbramod_add_cplpre
group: cf1
dataset: tuar
data_root: $PACLOCK_PROC/processed/tuar
model: cbramod_paclockfe
loader: null
pretrained: true
optimizer: adamw
lr: 0.0001
weight_decay: 0.05
epochs: 50
batch_size: 64
multi_lr: true
scheduler: cosine
grad_clip: 1.0
patience: 0
num_workers: 16
device: cuda
eval_every_steps: 0
model_kwargs:
  adapter: additive
  readout: native
  tokenizer_mode: pac_interaction
  interaction_mode: rotation
num_classes: 3
sample_rate: 200
loss: cross_entropy
seed: 0
label_smoothing: 0.1
max_hours: 24.0
