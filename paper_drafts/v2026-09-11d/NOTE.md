# paper_drafts/v2026-09-11d — tables in the CBraMod layout (Overleaf bd7c959, 20 pages)

Layout learned from CBraMod (ICLR 2025) Tables 2-3 / Appendix E:
- datasets as column groups (\multicolumn + \cmidrule), three metrics per dataset: BAcc / kappa / weighted F1 for
  multi-class, BAcc / AUC-PR / AUROC for binary; methods as rows, grouped supervised -> FM released -> FM scratch -> ours;
  mean ± std to four decimals; best per column bold; two datasets per table; main text carries the headline tasks,
  the appendix the rest in the same format.
Ours now: main text Table seizure (TUSZ, CHB-MIT) + Table events (TUEV, IIIC); appendix four tables for the other eight
corpora; ablation (duplex vs waveform-only x width) in the same 3-metric layout, two tables; design variants, CBraMod
rows, hosts tables in one consistent style. Every table is generated by scripts/gen_tables (results/tables/*.tex).
