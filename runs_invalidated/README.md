# Invalidated: wrong architectures

These BCI-IV-2a group-A results were produced by a from-scratch reimplementation
whose parameter counts did not match the xlsx (e.g. FFCL 0.70M vs 2.4M expected).
Group A was rebuilt on the official BIOT implementations
(paclock_bench/models/baselines/biot_official/). Kept only as a record; these
numbers must not enter the matrix.

Invalidated 2026-08-04.
